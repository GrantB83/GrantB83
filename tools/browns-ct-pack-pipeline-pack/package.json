{
  "name": "browns-ct-pack-pipeline-pack",
  "version": "1.0.0",
  "description": "Offline CLI orchestrator for Browns CT pack pipeline: booking-change-check → ct-pack-assemble → optional ct-pack-post-checklist",
  "type": "module",
  "main": "dist/index.js",
  "bin": {
    "pipeline": "dist/index.js"
  },
  "scripts": {
    "build": "tsc",
    "test": "node --test dist/**/*.test.js",
    "pipeline": "node dist/index.js",
    "test:fixtures": "npm run build && node dist/index.js --date 2026-09-20 --pack fixtures/sample-pack --outdir test-out && echo 'Fixture test completed successfully'",
    "clean": "rm -rf dist test-out out"
  },
  "keywords": [
    "browns",
    "dullstroom",
    "guest-comms",
    "whatsapp",
    "pipeline",
    "ct-pack"
  ],
  "author": "Grant Brown",
  "license": "MIT",
  "devDependencies": {
    "@types/node": "^20.0.0",
    "typescript": "^5.3.0"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
