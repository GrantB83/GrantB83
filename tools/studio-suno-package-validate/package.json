{
  "name": "studio-suno-package-validate",
  "version": "1.0.0",
  "description": "Offline CLI to validate Suno job packages before Studio spends browser time",
  "type": "module",
  "main": "dist/index.js",
  "bin": {
    "validate": "dist/index.js"
  },
  "scripts": {
    "build": "tsc",
    "test": "node --test dist/**/*.test.js",
    "validate": "node dist/index.js",
    "test:fixtures": "npm run build && node dist/index.js --dir fixtures/valid-job --outdir test-out && echo 'Fixture test completed successfully'",
    "clean": "rm -rf dist test-out"
  },
  "keywords": ["suno", "validation", "studio", "brownie-tunez"],
  "author": "Grant Brown",
  "license": "MIT",
  "devDependencies": {
    "@types/node": "^20.0.0",
    "typescript": "^5.3.0"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
