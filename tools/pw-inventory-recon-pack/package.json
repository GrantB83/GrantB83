{
  "name": "pw-inventory-recon-pack",
  "version": "1.0.0",
  "description": "Offline orchestrator CLI for Perfect Water inventory recon pack assembly",
  "type": "module",
  "main": "dist/index.js",
  "bin": {
    "pack": "dist/index.js"
  },
  "scripts": {
    "build": "tsc",
    "test": "node --test dist/**/*.test.js",
    "pack": "node dist/index.js",
    "test:fixtures": "npm run build && node dist/index.js --diff-outdir fixtures/prebuilt-diff --outdir test-out && echo 'Fixture test completed successfully'",
    "clean": "rm -rf dist test-out"
  },
  "keywords": ["perfect-water", "cos", "inventory", "recon", "pack", "grv", "stocktake", "diff"],
  "author": "Grant Brown",
  "license": "MIT",
  "devDependencies": {
    "@types/node": "^20.0.0",
    "typescript": "^5.3.0"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
