{
  "name": "pw-grv-csv-normalize",
  "version": "1.0.0",
  "description": "Offline GRV (goods received voucher) CSV normalizer for Perfect Water inventory operations",
  "type": "module",
  "main": "dist/index.js",
  "bin": {
    "normalize": "dist/index.js"
  },
  "scripts": {
    "build": "tsc",
    "test": "node --test dist/**/*.test.js",
    "normalize": "node dist/index.js",
    "test:fixtures": "npm run build && node dist/index.js --in fixtures/generic-good.csv --outdir test-out && node dist/index.js --in fixtures/loyverse-like.csv --outdir test-out-loyverse --profile loyverse && node dist/index.js --in fixtures/sparse-bad.csv --outdir test-out-bad && echo 'Fixture tests completed successfully'",
    "clean": "rm -rf dist test-out test-out-loyverse test-out-bad"
  },
  "keywords": ["grv", "goods-received", "csv", "normalize", "inventory", "perfect-water"],
  "author": "Grant Brown",
  "license": "MIT",
  "devDependencies": {
    "@types/node": "^20.0.0",
    "typescript": "^5.3.0"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
