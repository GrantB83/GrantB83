{
  "name": "attachment-filename-index-pipeline-pack",
  "version": "1.0.0",
  "description": "Offline CLI pipeline pack orchestrating attachment-filename-index for Vault / Family / CoS",
  "type": "module",
  "main": "dist/index.js",
  "bin": {
    "attachment-filename-index-pipeline": "dist/index.js"
  },
  "scripts": {
    "build": "tsc",
    "test": "npm run build && node --test dist/**/*.test.js",
    "pack": "node dist/index.js",
    "test:fixtures": "npm run build && npm run pack -- --files fixtures/sample-filenames.txt --outdir test-out && echo 'Fixture test completed successfully'",
    "clean": "rm -rf dist test-out out"
  },
  "keywords": [
    "attachment",
    "filename",
    "index",
    "pipeline",
    "vault",
    "family",
    "offline"
  ],
  "author": "Grant Brown <grant@thebrowns.co.za>",
  "license": "MIT",
  "devDependencies": {
    "@types/node": "^20.19.43",
    "typescript": "^5.9.3"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
