{
  "name": "vault-due-digest-pack",
  "version": "1.0.0",
  "description": "Offline CLI assembler for weekday Vault due digest by orchestrating vault-filename-due-queue and vault-entity-due-pack",
  "type": "module",
  "main": "dist/index.js",
  "bin": {
    "pack": "dist/index.js"
  },
  "scripts": {
    "build": "tsc",
    "test": "node --test dist/*.test.js",
    "pack": "node dist/index.js",
    "test:fixtures": "npm run build && node dist/index.js --queue fixtures/sample-queue.json --run-entity-pack --outdir test-out && echo '✅ Fixture test completed successfully'",
    "clean": "rm -rf dist test-out"
  },
  "keywords": ["vault", "due-date", "digest", "cipc", "sars", "trust", "compliance"],
  "author": "Grant Brown",
  "license": "MIT",
  "devDependencies": {
    "@types/node": "^20.0.0",
    "typescript": "^5.3.0"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
