{
  "name": "ledger-merchant-alias-suggest",
  "version": "1.0.0",
  "description": "Offline merchant→alias mapper using heuristic token overlap",
  "type": "module",
  "main": "dist/index.js",
  "bin": {
    "suggest": "dist/index.js"
  },
  "scripts": {
    "build": "tsc",
    "test": "node --test dist/src/*.test.js",
    "suggest": "node dist/index.js",
    "test:fixtures": "npm run build && npm run suggest -- --unmatched fixtures/unmatched.json --aliases fixtures/aliases.json --outdir test-out/unmatched && npm run suggest -- --merchants fixtures/merchants.txt --aliases fixtures/aliases.json --outdir test-out/merchants && echo 'Fixture tests completed successfully'",
    "clean": "rm -rf dist test-out"
  },
  "keywords": ["ledger", "merchant", "alias", "matcher", "suggest"],
  "author": "Grant Brown",
  "license": "MIT",
  "devDependencies": {
    "@types/node": "^20.0.0",
    "typescript": "^5.3.0"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
