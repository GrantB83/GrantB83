{
  "name": "browns-welcome-late-pipeline-pack",
  "version": "1.0.0",
  "description": "Offline CLI pipeline pack assembler combining browns-welcome-draft-pack, browns-late-checkin-queue, and optional browns-daily-ops-brief",
  "type": "module",
  "main": "dist/index.js",
  "bin": {
    "pack": "dist/index.js"
  },
  "scripts": {
    "build": "npx tsc",
    "test": "node --test dist/**/*.test.js",
    "pack": "node dist/index.js",
    "start": "node dist/index.js",
    "test:fixtures": "npm run build && npm run pack -- --bookings fixtures/sample-bookings.json --day 2026-09-20 --outdir test-out",
    "clean": "rm -rf dist test-out out"
  },
  "keywords": [
    "browns",
    "hospitality",
    "welcome",
    "late-checkin",
    "pipeline",
    "pack",
    "dullstroom"
  ],
  "author": "Grant Brown <grant@thebrowns.co.za>",
  "license": "MIT",
  "devDependencies": {
    "@types/node": "^20.0.0",
    "typescript": "^5.3.0"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
