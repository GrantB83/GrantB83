{
  "name": "vault-filename-due-queue",
  "version": "1.0.0",
  "description": "Offline CLI to extract due date hints from Vault/CIPC/SARS/trust filenames without opening bodies",
  "type": "module",
  "main": "dist/index.js",
  "bin": {
    "queue": "dist/index.js"
  },
  "scripts": {
    "build": "npx tsc",
    "test": "node --test dist/*.test.js",
    "queue": "node dist/index.js",
    "test:fixtures": "npm run build && node dist/index.js --files fixtures/sample-filenames.txt --outdir test-out && echo '✅ Fixture test completed successfully'",
    "clean": "rm -rf dist test-out"
  },
  "keywords": ["vault", "due-date", "cipc", "sars", "trust", "compliance", "filename"],
  "author": "Grant Brown",
  "license": "MIT",
  "devDependencies": {
    "@types/node": "^20.0.0",
    "typescript": "^5.3.0"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
