{
  "name": "vault-entity-due-pipeline-pack",
  "version": "1.0.0",
  "description": "Offline CLI pipeline pack orchestrator: vault-filename-due-queue → vault-entity-due-pack",
  "type": "module",
  "main": "dist/index.js",
  "scripts": {
    "build": "tsc",
    "pack": "node dist/index.js",
    "test": "node --test dist/**/*.test.js",
    "test:fixtures": "npm run build && node dist/index.js --filenames fixtures/healthy-pack/filenames.txt --outdir test-out",
    "clean": "rm -rf dist/ test-out/ out/"
  },
  "keywords": [
    "vault",
    "entity-pack",
    "pipeline",
    "offline",
    "cipc",
    "sars",
    "trust",
    "compliance"
  ],
  "author": "Grant Brown <grant@thebrowns.co.za>",
  "license": "MIT",
  "devDependencies": {
    "@types/node": "^20.0.0",
    "typescript": "^5.9.3"
  }
}
