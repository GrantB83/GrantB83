{
  "name": "family-school-subject-digest",
  "version": "1.0.0",
  "description": "Offline CLI to generate family school/admin digest from email subjects",
  "type": "module",
  "main": "dist/index.js",
  "bin": {
    "digest": "dist/index.js"
  },
  "scripts": {
    "build": "tsc",
    "test": "npm run build && node --test dist/classifier.test.js dist/parser.test.js",
    "digest": "node dist/index.js",
    "test:fixtures": "npm run build && npm run digest -- --input fixtures/subjects-mixed.txt --outdir test-out-mixed && npm run digest -- --input fixtures/subjects-school-only.txt --outdir test-out-school && npm run digest -- --input fixtures/subjects-sparse.txt --outdir test-out-sparse && echo 'All fixture tests completed successfully'",
    "clean": "rm -rf dist test-out* out*"
  },
  "keywords": ["family", "school", "AISD", "digest", "email"],
  "author": "Grant Brown",
  "license": "MIT",
  "devDependencies": {
    "@types/node": "^20.0.0",
    "typescript": "^5.3.0"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
