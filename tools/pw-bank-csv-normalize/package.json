{
  "name": "pw-bank-csv-normalize",
  "version": "1.0.0",
  "description": "Offline SA bank CSV normalizer for Perfect Water receipt recon",
  "type": "module",
  "main": "dist/index.js",
  "bin": {
    "normalize": "dist/index.js"
  },
  "scripts": {
    "build": "npx tsc",
    "test": "node --test dist/**/*.test.js",
    "normalize": "node dist/index.js",
    "test:fixtures": "npm run build && node dist/index.js --input fixtures/generic-good.csv --outdir test-out && node dist/index.js --input fixtures/fnb-like.csv --outdir test-out-fnb --profile fnb && node dist/index.js --input fixtures/xero-import.csv --outdir test-out-xero --profile xero-import && echo 'Fixture tests completed successfully'",
    "clean": "rm -rf dist test-out test-out-fnb test-out-xero"
  },
  "keywords": ["bank", "csv", "normalize", "xero", "reconciliation", "perfect-water"],
  "author": "Grant Brown",
  "license": "MIT",
  "devDependencies": {
    "@types/node": "^20.0.0",
    "typescript": "^5.3.0"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
