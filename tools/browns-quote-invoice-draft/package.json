{
  "name": "browns-quote-invoice-draft",
  "version": "1.0.0",
  "description": "Offline CLI that drafts quote and invoice/proforma email+WhatsApp text from a booking/quote JSON",
  "type": "module",
  "main": "dist/index.js",
  "bin": {
    "draft": "dist/index.js"
  },
  "scripts": {
    "build": "tsc",
    "test": "node --test dist/**/*.test.js",
    "draft": "node dist/index.js",
    "test:fixtures": "npm run build && node dist/index.js --quote fixtures/sample-quote.json --outdir test-out && node dist/index.js --quote fixtures/sample-quote-no-amounts.json --outdir test-out-no-amounts && echo 'Fixture tests completed successfully'",
    "clean": "rm -rf dist test-out test-out-no-amounts"
  },
  "keywords": ["browns", "hospitality", "quote", "invoice", "proforma", "draft"],
  "author": "Grant Brown",
  "license": "MIT",
  "devDependencies": {
    "@types/node": "^20.0.0",
    "typescript": "^5.3.0"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
