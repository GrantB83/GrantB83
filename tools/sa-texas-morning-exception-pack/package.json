{
  "name": "sa-texas-morning-exception-pack",
  "version": "1.0.0",
  "description": "Offline CLI to assemble SA Texas-morning exception digest for Heavy Metal + hospitality / The Browns",
  "type": "module",
  "main": "dist/index.js",
  "bin": {
    "pack": "dist/index.js"
  },
  "scripts": {
    "build": "tsc",
    "test": "node --test dist/**/*.test.js",
    "pack": "node dist/index.js",
    "test:fixtures": "npm run build && node dist/index.js --date 2026-09-02 --browns-bookings fixtures/sample-bookings.json --hm-quotes-dir fixtures/hm-quotes --notes fixtures/sample-notes.md --outdir test-out && echo 'Fixture test completed successfully'",
    "clean": "rm -rf dist test-out out"
  },
  "keywords": ["sa-ops", "cos", "heavy-metal", "browns", "dullstroom", "exception-digest"],
  "author": "Grant Brown",
  "license": "MIT",
  "devDependencies": {
    "@types/node": "^20.0.0",
    "typescript": "^5.3.0"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
