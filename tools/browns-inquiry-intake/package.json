{
  "name": "browns-inquiry-intake",
  "version": "1.0.0",
  "description": "Offline CLI to extract structured booking/quote data from freeform inquiry text",
  "type": "module",
  "main": "dist/index.js",
  "bin": {
    "intake": "dist/index.js"
  },
  "scripts": {
    "build": "tsc",
    "test": "node --test dist/**/*.test.js",
    "intake": "node dist/index.js",
    "test:fixtures": "npm run build && node dist/index.js --text fixtures/sample-inquiry.txt --outdir test-out && echo 'Fixture test completed successfully'",
    "clean": "rm -rf dist test-out out"
  },
  "keywords": ["browns", "hospitality", "booking", "inquiry", "dullstroom"],
  "author": "Grant Brown",
  "license": "MIT",
  "devDependencies": {
    "@types/node": "^20.0.0",
    "typescript": "^5.3.0"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
