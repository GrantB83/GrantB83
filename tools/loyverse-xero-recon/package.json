{
  "name": "loyverse-xero-recon",
  "version": "1.0.0",
  "description": "Offline Loyverse ↔ Xero reconciliation gap CLI",
  "type": "module",
  "main": "dist/index.js",
  "bin": {
    "recon": "dist/index.js"
  },
  "scripts": {
    "build": "tsc",
    "test": "node --test dist/**/*.test.js",
    "recon": "node dist/index.js",
    "recon:summary": "node dist/index.js --mode summary",
    "test:fixtures": "npm run build && node dist/index.js --mode receipt --loyverse fixtures/loyverse-sales.csv --xero fixtures/xero-sales.csv --output test-out && echo 'Receipt mode fixture test completed successfully'",
    "test:summary": "npm run build && node dist/index.js --mode summary --loyverse fixtures/summary/ --xero fixtures/summary/ --output test-summary-out && echo 'Summary mode fixture test completed successfully'",
    "clean": "rm -rf dist test-out test-summary-out"
  },
  "keywords": ["loyverse", "xero", "reconciliation", "accounting"],
  "author": "Grant Brown",
  "license": "MIT",
  "devDependencies": {
    "@types/node": "^20.0.0",
    "typescript": "^5.3.0"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
