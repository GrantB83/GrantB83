{
  "name": "hm-delivery-pod-draft",
  "version": "1.0.0",
  "description": "Offline CLI to generate DRAFT proof-of-delivery notes from Heavy Metal delivery data",
  "type": "module",
  "main": "dist/index.js",
  "bin": {
    "draft": "dist/index.js"
  },
  "scripts": {
    "build": "tsc",
    "test": "node --test dist/**/*.test.js",
    "draft": "node dist/index.js",
    "test:fixtures": "npm run build && node dist/index.js --pod fixtures/sample-pod.json --outdir test-out && echo 'Fixture test completed successfully'",
    "clean": "rm -rf dist test-out out"
  },
  "keywords": ["heavy-metal", "sand", "stone", "delivery", "proof-of-delivery", "pod", "dullstroom"],
  "author": "Grant Brown",
  "license": "MIT",
  "devDependencies": {
    "@types/node": "^20.0.0",
    "typescript": "^5.3.0"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
