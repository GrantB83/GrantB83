{
  "name": "csv-fixture-harness",
  "version": "1.0.0",
  "description": "Offline CLI validator for CSV fixtures: headers, required columns, row counts, blank detection, and currency checks",
  "type": "module",
  "main": "dist/index.js",
  "bin": {
    "check": "dist/index.js"
  },
  "scripts": {
    "build": "tsc",
    "test": "node --test dist/**/*.test.js",
    "check": "node dist/index.js",
    "test:fixtures": "npm run build && node dist/index.js --csv fixtures/good.csv --require-headers Date,Amount && echo 'Fixture test completed successfully'",
    "clean": "rm -rf dist test-out"
  },
  "keywords": ["csv", "validation", "fixture", "testing", "data-quality"],
  "author": "Grant Brown",
  "license": "MIT",
  "devDependencies": {
    "@types/node": "^20.0.0",
    "typescript": "^5.3.0"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
