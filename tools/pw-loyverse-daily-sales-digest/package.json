{
  "name": "pw-loyverse-daily-sales-digest",
  "version": "1.0.0",
  "description": "Offline Perfect Water Loyverse daily sales digest CLI",
  "type": "module",
  "main": "dist/index.js",
  "bin": {
    "digest": "dist/index.js"
  },
  "scripts": {
    "build": "tsc",
    "test": "node --test dist/**/*.test.js",
    "digest": "node dist/index.js",
    "test:fixtures": "npm run build && node dist/index.js --csv fixtures/loyverse-day.csv --outdir test-out && echo 'Fixture test completed successfully'",
    "clean": "rm -rf dist test-out"
  },
  "keywords": ["loyverse", "perfect-water", "sales", "digest"],
  "author": "Grant Brown",
  "license": "MIT",
  "devDependencies": {
    "@types/node": "^20.0.0",
    "typescript": "^5.3.0"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
