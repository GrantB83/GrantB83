{
  "name": "browns-late-checkin-queue",
  "version": "1.0.0",
  "description": "Offline CLI to generate late/after-hours check-in queue for CoS 09:00 CT pack",
  "type": "module",
  "main": "dist/index.js",
  "bin": {
    "queue": "dist/index.js"
  },
  "scripts": {
    "build": "tsc",
    "test": "node --test dist/**/*.test.js",
    "queue": "node dist/index.js",
    "test:fixtures": "npm run build && node dist/index.js --bookings fixtures/sample-bookings.json --day 2026-09-20 --outdir test-out && echo 'Fixture test completed successfully'",
    "clean": "rm -rf dist test-out out"
  },
  "keywords": ["browns", "dullstroom", "guesthouse", "late-checkin", "after-hours", "operations", "hospitality"],
  "author": "Grant Brown",
  "license": "MIT",
  "devDependencies": {
    "@types/node": "^20.0.0",
    "typescript": "^5.3.0"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
