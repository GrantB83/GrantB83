{
  "name": "family-calendar-ics-digest",
  "version": "1.0.0",
  "description": "Offline CLI to parse .ics calendar exports and generate Family/CoS morning digest",
  "type": "module",
  "main": "dist/index.js",
  "bin": {
    "digest": "dist/index.js"
  },
  "scripts": {
    "build": "tsc",
    "test": "npm run build && node --test dist/parser.test.js dist/digest.test.js",
    "digest": "node dist/index.js",
    "test:fixtures": "npm run build && npm run digest -- --ics fixtures/school-calendar.ics --from 2026-09-02 --to 2026-09-05 --outdir test-out-school && npm run digest -- --ics fixtures/empty-calendar.ics --from 2026-09-01 --to 2026-09-30 --outdir test-out-empty && echo 'All fixture tests completed successfully'",
    "clean": "rm -rf dist test-out* out*"
  },
  "keywords": ["family", "calendar", "ics", "digest", "offline"],
  "author": "Grant Brown",
  "license": "MIT",
  "dependencies": {
    "ical.js": "2.0.1"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "typescript": "^5.3.0"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
